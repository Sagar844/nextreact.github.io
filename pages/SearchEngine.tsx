import { NextPage } from "next";
import React from "react";
type SearchEngineProps = {
  data: string;
};

const SearchEngine: NextPage<SearchEngineProps> = ({ data }) => {
  return <div></div>;
};

export default SearchEngine;
